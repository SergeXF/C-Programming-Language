int main()
{
	int i, j, k;
	i = 5; j = 10; k = 1;
	printf("%d", k > i < j);
	return 0;
}
//这是因为 C 语言的运算符优先级和结合性的规则。在你的代码中，k>i<j 实际上被解析为 (k>i)<j。
//如果你想要检查 k 是否在 i 和 j 之间，你应该写成 i < k && k < j。这样才能得到预期的结果。
