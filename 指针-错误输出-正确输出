int main()
{
	int a = 10;
	int* p = &a;
	printf("%p\n", a);
	printf("%d\n", p);//两次输出的区别？？？？？？？
//你的代码中有两个printf函数调用，它们的输出结果会有所不同。
//printf("%p\n", a); ：这里你试图打印变量a的地址，但是你传入的是a的值，而不是地址。正确的做法应该是printf("%p\n", &a); ，这样就可以打印出a的地址了。
//printf("%d\n", p); ：这里你试图打印指针p的值，但是p的值实际上是一个地址（即a的地址），所以应该使用% p来打印，即printf("%p\n", p); 。
//总的来说，% p用于打印地址，% d用于打印整数。在你的代码中，p是一个指针，它存储的是地址，而a是一个整数。所以，当你试图打印一个地址时，应该使用% p，当你试图打印一个整数时，应该使用% d。
	printf("%p\n", &a);//正确输出
	printf("%p\n", p);
	return 0;
}
