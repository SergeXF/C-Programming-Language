int my_max(int x, int y)
{
	int z;
	if (x > y)z = x;
	else z = y;
	return(z);
}
int main()
{
	int my_max(int x, int y);
	int a, b, c;
	scanf("%d %d", &a, &b);
	c = my_max(a,b);
	printf("max=%d\n", c);
	return 0;
}
